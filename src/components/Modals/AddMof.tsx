import React from 'react'
import { AddMofPropsInterface } from '../../Types/UtilsTypes'

export const AddMof: React.FC<AddMofPropsInterface>= ({cenoteId, theme, category, showModal, handleToggleModal, refetch}) => {
  return (
    <div>AddMof</div>
  )
}

